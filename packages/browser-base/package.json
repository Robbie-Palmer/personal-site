{
  "name": "browser-base",
  "version": "0.1.0",
  "private": true,
  "description": "Browser platform extensions shared across applications",
  "type": "module",
  "exports": {
    "./errors": "./src/errors.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
