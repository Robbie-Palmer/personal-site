{
  "name": "node-base",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js platform extensions shared across applications and tooling",
  "type": "module",
  "exports": {
    "./env": "./src/env.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
