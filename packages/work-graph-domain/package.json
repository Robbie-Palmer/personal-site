{
  "name": "work-graph-domain",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free domain rules for the Work Graph",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
