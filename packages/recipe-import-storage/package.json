{
  "name": "recipe-import-storage",
  "version": "0.1.0",
  "private": true,
  "description": "Object storage key contracts for the recipe import workflow",
  "type": "module",
  "exports": {
    "./keys": "./src/keys.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
