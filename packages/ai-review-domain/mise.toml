[tasks.typecheck]
description = "Typecheck the shared AI review domain package"
depends = ["//:install"]
run = "pnpm typecheck"

[tasks.test]
description = "Test the shared AI review domain package"
depends = ["//:install"]
run = "pnpm test"

[tasks.check]
description = "Check the shared AI review domain package"
depends = ["typecheck", "test"]
