import "@/components/projects/pitch-deck/pitch-deck-presenter.css";

export default function ProjectDeckPresenterLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
