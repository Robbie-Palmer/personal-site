apiVersion: v1
kind: Service
metadata:
  name: t3-code
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: t3-code
  ports:
    - name: http
      port: 3773
      targetPort: http
      protocol: TCP
