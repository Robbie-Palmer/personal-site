apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: t3-code
spec:
  tokenSecret:
    name: doppler-token
  managedSecret:
    name: t3-code-runtime
    namespace: t3-code-pilot
  project: homelab
  config: prd_remote_development_pilot
  resyncSeconds: 300
