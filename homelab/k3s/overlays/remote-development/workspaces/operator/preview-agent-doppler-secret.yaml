apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: t3-code-preview-access
spec:
  tokenSecret:
    name: doppler-agent-token
  managedSecret:
    name: t3-code-preview-access
    namespace: t3-code
  project: personal-site
  config: dev_agent
  resyncSeconds: 300
