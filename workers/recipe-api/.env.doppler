# Intentionally empty.
#
# Wrangler dev normally prefers .dev.vars when it exists. The dev script passes
# this empty file with --env-file so local secrets come from Doppler-injected
# process.env instead.
