# Copy to .env only when bypassing the Doppler wrapper for local diagnosis.
# Prefer: doppler run --project homelab --config prd_remote_development_infra
HCLOUD_TOKEN=
TF_API_TOKEN=

# Public material and non-secret bootstrap policy.
TF_VAR_ssh_public_key=
TF_VAR_bootstrap_ssh_cidrs=[]
